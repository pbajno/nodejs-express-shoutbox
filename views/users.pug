extends layout

block content
	h1 #{title}
	ul.list-group
		each user, i in users
			li.list-group-item
				a(href="/user/"+user.username)
					img(src='uploads/80/'+user.avatar, alt="Avatar", height='25', class='rounded')
					=user.username